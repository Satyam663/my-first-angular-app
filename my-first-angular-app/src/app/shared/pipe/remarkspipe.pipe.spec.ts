import { RemarkspipePipe } from './remarkspipe.pipe';

describe('RemarkspipePipe', () => {
  it('create an instance', () => {
    const pipe = new RemarkspipePipe();
    expect(pipe).toBeTruthy();
  });
});
